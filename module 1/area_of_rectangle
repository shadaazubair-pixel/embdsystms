#include<stdio.h>
int main()
{
    float length, breadth, area;
    printf("enter the length:");
    scanf("%f",&length);

    printf("\nenter the breadth:");
    scanf("%f",&breadth);

    area=length*breadth;
    printf("the area of rectangle is :%.2f",area);
    return 0;
}