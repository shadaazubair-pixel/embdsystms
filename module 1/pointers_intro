#include<stdio.h>
int a=10;
int*ptr =&a;
int main(){
    printf("value of a=%d",a);
    printf("Adress of a=%p",&a);
    printf("adress of variable using pointer p=%p",ptr);
    printf("value of inside address stored in p=%d\n",*ptr);
    printf("adress of p=%p",&ptr);

    return 0;
}