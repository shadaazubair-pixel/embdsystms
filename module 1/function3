#include<stdio.h>
int largest(int a, int b){
    if(a>b){
        printf("the largest is %d\n",a);
        return a;
    }
    else
    printf("the largest is %d\n",b);
    return b;
}



int main(){
    int x,y;
    printf("enter the nos:");
    scanf("%d %d",&x,&y);
    largest(x,y);
}