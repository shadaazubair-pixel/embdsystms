#include<stdio.h>
int main(){
    int arr[5]={1,2,3,4,5};
    int *arrptr=arr;

    for(i=0;i<5;i++){
        printf("the value of[%d]=%d and the address is %d",i,(*arrptr)+5,arrptr);
    }
    return 0;

}