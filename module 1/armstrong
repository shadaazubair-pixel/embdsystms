#include<stdio.h>
int main(){
    int n,sum=0,rem;
    printf("enter the three digit number:");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        rem=n%10;
        sum=(sum+rem)^3;
        n=n/10;
    }
    return 0;
}