#include<stdio.h>
int main(){
    int i,j,rows,space;
    printf("%d",row);
    scanf("%d",&row);
    for(i=1;i<=row;i++){
        for(space=1;space<=row-1;space++){
            printf(" ");
        }
        for(j=1;j<=i;j++){
            printf("* ");
        }
        printf("\n");
    }
    return 0;

}